package controller;

/**
 * Represents the interface that the controller in our MVC program inherits. The controller
 * has a void start() method, which gives the controller control.
 */
public interface IController {

  void start();
}
